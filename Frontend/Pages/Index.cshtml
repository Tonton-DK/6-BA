@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div class="align-left">
    <br/>
    <h3>Services:</h3>
    <br/>
    <ul>
        @{
            if(Model.ServiceStatus != null)
            {
                foreach (var status in Model.ServiceStatus)
                {
                    <li>
                        <h4>@status.Key.Name </h4>
                        <p>Status: @(status.Value ? "Found" : "Missing")</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Users:</h3>
    <br/>
    <ul>
        @{
            if(Model.Client != null)
            {
                <li>
                    <h4>Client: @Model.Client.Id</h4>
                    <p>First Name: @Model.Client.FirstName</p>
                    <p>Last Name: @Model.Client.LastName</p>
                </li>
            }
            if(Model.Provider != null)
            {
                <li>
                    <h4>Client: @Model.Provider.Id</h4>
                    <p>First Name: @Model.Provider.FirstName</p>
                    <p>Last Name: @Model.Provider.LastName</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Created Job:</h3>
    <br/>
    <ul>
        @{
            if(Model.Job != null)
            {
                <li>
                    <h4>@Model.Job.Id</h4>
                    <p>Name: @Model.Job.Title</p>
                    <p>Description: @Model.Job.Description</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Jobs listed:</h3>
    <br/>
    <ul>
        @{
            if(Model.Jobs != null)
            {
                foreach (var job in Model.Jobs)
                {
                    <li>
                        <h4>@job.Id</h4>
                        <p>Name: @job.Title</p>
                        <p>Description: @job.Description</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Client Jobs listed:</h3>
    <br/>
    <ul>
        @{
            if(Model.ClientJobs != null)
            {
                foreach (var job in Model.ClientJobs)
                {
                    <li>
                        <h4>@job.Id</h4>
                        <p>Name: @job.Title</p>
                        <p>Description: @job.Description</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Created Offer:</h3>
    <br/>
    <ul>
        @{
            if(Model.Offer != null)
            {
                <li>
                    <h4>@Model.Offer.Id</h4>
                    <p>Name: @Model.Offer.Price</p>
                    <p>Description: @Model.Offer.Duration</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Created Counter Offer:</h3>
    <br/>
    <ul>
        @{
            if(Model.CounterOffer != null)
            {
                <li>
                    <h4>@Model.CounterOffer.Id</h4>
                    <p>Name: @Model.CounterOffer.Price</p>
                    <p>Description: @Model.CounterOffer.Duration</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Offers listed:</h3>
    <br/>
    <ul>
        @{
            if(Model.Offers != null)
            {
                foreach (var offer in Model.Offers)
                {
                    <li>
                        <h4>@offer.Id</h4>
                        <p>Name: @offer.Price</p>
                        <p>Description: @offer.Duration</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Created Contract:</h3>
    <br/>
    <ul>
        @{
            if(Model.Contract != null)
            {
                <li>
                    <h4>@Model.Contract.Id</h4>
                    <p>Name: @Model.Contract.ContractState</p>
                    <p>Description: @Model.Contract.CreationDate</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Contracts listed:</h3>
    <br/>
    <ul>
        @{
            if(Model.Contracts != null)
            {
                foreach (var contract in Model.Contracts)
                {
                    <li>
                        <h4>@contract.Id</h4>
                        <p>Name: @contract.ContractState</p>
                        <p>Description: @contract.CreationDate</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Created Review:</h3>
    <br/>
    <ul>
        @{
            if(Model.Review != null)
            {
                <li>
                    <h4>@Model.Review.Id</h4>
                    <p>Name: @Model.Review.Rating</p>
                    <p>Description: @Model.Review.Comment</p>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Reviews listed:</h3>
    <br/>
    <ul>
        @{
            if(Model.Reviews != null)
            {
                foreach (var review in Model.Reviews)
                {
                    <li>
                        <h4>@review.Id</h4>
                        <p>Name: @review.Rating</p>
                        <p>Description: @review.Comment</p>
                    </li>
                }
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Client Rating:</h3>
    <br/>
    <ul>
        @{
            if(Model.ClientRating != null)
            {
                <li>
                    <h4>@Model.ClientRating</h4>
                </li>
            }
        }
    </ul>
</div>

<div class="align-left">
    <br/>
    <h3>Provider Rating:</h3>
    <br/>
    <ul>
        @{
            if(Model.ProviderRating != null)
            {
                <li>
                    <h4>@Model.ProviderRating</h4>
                </li>
            }
        }
    </ul>
</div>
